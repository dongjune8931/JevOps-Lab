# JevOps-Lab 작업 지침

이 파일의 규칙은 저장소 전체에 적용한다.

## 작업 시작

- 새 프로젝트·실험을 만들거나 저장소 운영 방식을 변경할 때는 `docs/WORKING_AGREEMENT.md`를 읽는다.
- 기존 프로젝트·실험을 작업할 때는 해당 폴더의 `README.md`, `PLAN.md`, `DECISIONS.md`, `COSTS.md`를 먼저 확인한다.
- 하나의 Codex 세션과 PR은 원칙적으로 하나의 프로젝트 또는 실험만 다룬다.

## 경계와 폴더

- 프로젝트는 `projects/P###-slug/`, 독립 실험은 `experiments/E###-slug/`에 둔다.
- 모든 ID와 slug는 저장소 전체에서 유일해야 한다.
- 다른 프로젝트나 실험의 파일을 같은 PR에서 수정하지 않는다.
- 둘 이상의 작업 단위에서 실제로 재사용하기 전에는 코드를 `shared/`로 이동하지 않는다.

## 문서

- 각 프로젝트와 실험은 자체 `README.md`, `PLAN.md`, `DECISIONS.md`, `COSTS.md`를 유지한다.
- 작업 결과, Jev 활용 방식, 검증 방법, 한계, 비용을 해당 `README.md`에 반영한다.
- 프로젝트나 실험의 상태 또는 결과가 바뀌면 루트 `README.md`의 카탈로그도 같은 PR에서 갱신한다.
- 세션 종료 전 `PLAN.md`에 완료한 작업, 마지막 검증 결과, 다음 작업, 블로커를 기록한다.
- 결과 수치는 원본 결과 파일이나 재현 가능한 명령으로 추적할 수 있어야 한다.

## Git과 PR

- `main`에 직접 커밋하지 않는다. 최신 `origin/main`에서 작업 브랜치를 만든다.
- 브랜치, 커밋, PR 규칙은 `docs/WORKING_AGREEMENT.md`를 따른다.
- 모든 변경은 검증 후 PR을 생성하고, 필수 검사가 통과하면 squash merge한 뒤 원격 브랜치를 삭제한다.
- 저장소 공통 변경에는 `repository.md`, 프로젝트에는 해당 프로젝트 전용 템플릿, 실험에는 해당 실험 전용 템플릿을 사용한다.
- 인증, 권한, 필수 검사 실패 등으로 PR 생성 또는 머지가 불가능하면 우회하거나 `main`에 직접 반영하지 말고 사용자에게 알린다.

## 비용과 비밀정보

- 기본값은 비용이 발생하지 않는 로컬·오픈소스 실행이다.
- API 호출, 클라우드 리소스, 유료 SaaS 등 0원이 아닐 수 있는 작업은 실행 전에 예상 비용·상한·목적·무료 대안을 제시하고 사용자의 명시적 승인을 받는다.
- 비용 승인은 해당 작업에만 유효하다. 승인받은 비용과 실제 비용은 작업 단위의 `COSTS.md`에 기록한다.
- API 키, 토큰, kubeconfig, 인증서 등 비밀정보를 커밋하지 않는다.
- 실제 운영 로그나 telemetry를 외부 API에 보내지 않는다. 필요한 경우 먼저 민감정보를 제거하고 사용자의 명시적 승인을 받는다.

## 실험과 재현성

- 실험 전에 가설, 비교군, 평가 지표, 성공·반증 조건을 문서화한다.
- 실행 날짜, Git commit SHA, 데이터셋 버전, 모델 실제 버전, 설정, 반복 횟수, random seed를 결과와 함께 기록한다.
- 성공한 결과만 선택하지 않는다. 실패한 run, 제외한 데이터, 제외 이유도 보존한다.
- `latest` 모델 alias를 사용하더라도 응답에 포함된 실제 모델 버전을 기록한다.

## 환경과 정리

- 로컬·격리 환경을 기본으로 하며 운영 환경 접근이나 변경은 별도 승인을 받는다.
- 생성한 Kubernetes·클라우드·SaaS 리소스에는 재현 가능한 teardown 절차가 있어야 한다.
- 작업 종료 전 남은 리소스와 과금 종료 여부를 확인한다. 자동 정리가 실패하면 사용자에게 알린다.

## 규칙 개선

- 반복되는 문제나 여러 작업 단위에 필요한 규칙을 발견하면 공통 문서 개선을 제안한다.
- 프로젝트나 실험 PR에 저장소 공통 규칙 변경을 섞지 않고 별도의 `docs(repo)` PR로 분리한다.
