global:
  resolve_timeout: 1m
route:
  receiver: p001-local
  group_by: [alertname]
  group_wait: 1s
  group_interval: 5s
  repeat_interval: 1h
receivers:
  - name: p001-local
    webhook_configs:
      - url: http://webhook:8080/alerts
        send_resolved: true
